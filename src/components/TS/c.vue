<template>
  <div>
    参考地址：https://juejin.cn/post/7068081327857205261#heading-24
  <pre>
    <h5>interface 基本概念</h5>
    interface 用于描述对象的基本形状和结构
    定义对象属性要和定义的类型保持一致，不然会报错，在interface接口中可以引用以下字段来控制接口描述
     ? 可选属性
     as 强制转换为某个接口类型
     readonly：只读属性，不可修改
     [key: number]: any 类数组（不是真数组）任意类型（自定义类型）
     [key: string]: any 对象任意类型（自定义类型）

    <h5></h5>
    <h5></h5>
    <h5></h5>
    <h5></h5>
    <h5></h5>
  </pre>
  </div>
</template>